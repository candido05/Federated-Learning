# --- Passo 1: Criar o Ambiente Virtual ---
# No diretório do projeto:
python3 -m venv venv
source venv/bin/activate

# --- Passo 2: Instalar Dependências ---
pip install --upgrade pip
pip install flwr xgboost scikit-learn numpy

# --- Passo 3: Configuração para GNS3 ---
# Certifique-se de que as VMs tenham conectividade de rede (ping entre elas).
# O Flower SuperLink (servidor) deve escutar em 0.0.0.0 para aceitar conexões externas.
# O Flower SuperNode (cliente) deve apontar para o IP real da VM Servidor.